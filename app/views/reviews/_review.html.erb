<div class="pull-left">
    <p ><%= review.body %></p>

</div>
<br>
<div class="pull-right">
    <p>
        <small>Submitted
            by
            <%= review.user.email %></small>
    </p>
</div>
<br>
<br>

  <div class="review-delete">
  <% if review.user == current_user %>
 <%= link_to("Delete", product_review_path(@product, review), method: :delete, data: { confirm: "您确定要删除这条评论?"}, class: "btn btn-sm pull-right") %>
  <% end %>
  <br>
<hr>

  </div>
